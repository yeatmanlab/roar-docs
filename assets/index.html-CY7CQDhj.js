import{_ as l,c as p,a as s,b as t,d as a,e as o,f as r,r as i,o as c}from"./app-H3n9Pky_.js";const h="/roar-docs/assets/emulator-overview-D7EUrMav.png",u="/roar-docs/assets/pgweb-connection-form-E82bQJTZ.png",m="/roar-docs/assets/emulator-storage-bucket-B1OWIXvo.png",g="/roar-docs/assets/emulator-storage-recordings-Cxb-TpaS.png",v={};function b(k,e){const n=i("RouteLink"),d=i("Mermaid");return c(),p("div",null,[e[12]||(e[12]=s("h1",{id:"assessment-environment",tabindex:"-1"},[s("a",{class:"header-anchor",href:"#assessment-environment"},[s("span",null,"Assessment Environment")])],-1)),e[13]||(e[13]=s("p",null,"Run any ROAR assessment on your own machine, play through it, and query the data it produces — with a real database, the real backend, and no cloud credentials.",-1)),e[14]||(e[14]=s("p",null,[t("This page is for researchers who want to "),s("strong",null,"produce and inspect assessment data locally"),t(": to try a new task variant, check what a run actually records, or generate test data without touching a shared environment. It is the local counterpart to the cloud data workflow in "),s("a",{href:"https://yeatmanlab.github.io/roar-docs/researcher/data-tools/",target:"_blank",rel:"noopener noreferrer"},"Data Tools"),t(" — nothing here writes to BigQuery or to any shared bucket.")],-1)),s("p",null,[e[1]||(e[1]=t("If you already know your way around and just need the commands, connection details, or a query to paste, go straight to the ")),a(n,{to:"/researcher/assessment-environment/quick-reference.html"},{default:o(()=>e[0]||(e[0]=[t("Quick Reference")])),_:1}),e[2]||(e[2]=t("."))]),e[15]||(e[15]=r('<div class="hint-container tip"><p class="hint-container-title">Canonical sources</p><p>Two documents in the repository are the source of truth and are maintained alongside the code. This page is an orientation guide; where they disagree, the repository wins.</p><ul><li><a href="https://github.com/yeatmanlab/roar-dashboard/blob/main/apps/assessments/ASSESSMENT_ENVIRONMENT.md" target="_blank" rel="noopener noreferrer">Setup &amp; Operations guide</a> — installing, starting, seeding, updating, troubleshooting</li><li><a href="https://github.com/yeatmanlab/roar-dashboard/blob/main/apps/assessments/ASSESSMENT_RESEARCH_GUIDE.md" target="_blank" rel="noopener noreferrer">Research guide</a> — producing runs, every query, metadata, CSV export, recordings</li></ul></div><h2 id="what-this-gives-you" tabindex="-1"><a class="header-anchor" href="#what-this-gives-you"><span>What this gives you</span></a></h2><ul><li><strong>Every assessment in one place.</strong> Eleven assessments live in the monorepo under <code>apps/assessments/</code>, and they all start the same way.</li><li><strong>Two commands to a running assessment</strong> — <code>npm run setup</code> once, then <code>npm start</code>.</li><li><strong>The real backend against a production-shaped schema.</strong> This is not a mock or a simulator, so what you see locally is what the platform records.</li><li><strong>Your data is yours.</strong> It is local and private, and you query it with ordinary SQL.</li></ul><h2 id="the-research-loop" tabindex="-1"><a class="header-anchor" href="#the-research-loop"><span>The research loop</span></a></h2>',4)),a(d,{id:"mermaid-57",code:""}),e[16]||(e[16]=r(`<p>Everything below supports that loop.</p><h2 id="before-you-start" tabindex="-1"><a class="header-anchor" href="#before-you-start"><span>Before you start</span></a></h2><p>Clone the monorepo if you have not already:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> clone https://github.com/yeatmanlab/roar-dashboard.git</span>
<span class="line"><span class="token builtin class-name">cd</span> roar-dashboard</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>You will also need:</p><ul><li><strong>Node dependencies</strong> — installed from the repository root; <code>npm run setup</code> does this for you.</li><li><strong>Docker</strong> with Compose v2 (<code>docker compose version</code> should work). <ul><li>macOS: <code>brew install --cask docker</code>, then launch Docker Desktop.</li><li>Ubuntu/Debian: <code>curl -fsSL https://get.docker.com | sh</code>, then <code>sudo usermod -aG docker $USER</code> and log out and back in.</li></ul></li><li><strong>Port 5433 free</strong> — the local database publishes there by default.</li></ul><p><code>npm run setup</code> checks the last two for you and prints fix-it instructions, so you do not have to verify them by hand.</p><h2 id="first-time-setup" tabindex="-1"><a class="header-anchor" href="#first-time-setup"><span>First-time setup</span></a></h2><p>Run this once, from the directory of the assessment you care about:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">cd</span> apps/assessments/roar-swr</span>
<span class="line"><span class="token function">npm</span> run setup</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>It walks through four steps:</p><ol><li><strong>Checks Docker</strong> (Compose v2), printing install options if it is missing.</li><li><strong>Checks that port 5433 is free</strong> for the local database.</li><li><strong>Installs dependencies and builds the platform libraries.</strong> This is the slow step — expect a few minutes.</li><li><strong>Creates your <code>taskVariantParameters.json</code></strong> from the committed example. It never overwrites an existing one.</li></ol><p>Docker and the port are <em>checked but not required</em> to finish, so you can prepare the repository now and sort out Docker later. Any blocker is repeated in a summary at the end.</p><h2 id="everyday-use" tabindex="-1"><a class="header-anchor" href="#everyday-use"><span>Everyday use</span></a></h2><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">npm</span> start</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>That is all you need from then on. It brings the shared services up if they are not already running, then starts this assessment&#39;s dev server. Open <strong><a href="http://localhost:8000" target="_blank" rel="noopener noreferrer">http://localhost:8000</a></strong> to play.</p><h3 id="one-shared-stack-many-assessments" tabindex="-1"><a class="header-anchor" href="#one-shared-stack-many-assessments"><span>One shared stack, many assessments</span></a></h3><p>The database, the backend, and the Firebase emulators are <strong>one shared Docker stack</strong> that keeps running in the background. Only the dev server on port 8000 is per-assessment, and only one assessment runs at a time.</p><p>Three things follow from that:</p><ol><li>Switching assessments tears nothing down — only the dev server changes.</li><li>Runs from several assessments coexist in the same database.</li><li>Each additional assessment needs its variants seeded once, because automatic seeding only happens for whichever assessment brought the stack up.</li></ol><h3 id="what-s-running-and-where" tabindex="-1"><a class="header-anchor" href="#what-s-running-and-where"><span>What&#39;s running, and where</span></a></h3><table><thead><tr><th>Process</th><th>URL</th></tr></thead><tbody><tr><td>Assessment dev server — <em>where you play</em></td><td><a href="http://localhost:8000" target="_blank" rel="noopener noreferrer">http://localhost:8000</a></td></tr><tr><td>ROAR backend</td><td><a href="http://localhost:4000" target="_blank" rel="noopener noreferrer">http://localhost:4000</a></td></tr><tr><td>PostgreSQL — <em>where you query</em></td><td><code>localhost:5433</code></td></tr><tr><td>Firebase Auth emulator</td><td><a href="http://localhost:9099" target="_blank" rel="noopener noreferrer">http://localhost:9099</a></td></tr><tr><td>Firebase Storage emulator</td><td><a href="http://localhost:9199" target="_blank" rel="noopener noreferrer">http://localhost:9199</a></td></tr><tr><td>Firebase Emulator UI — <em>browse recordings</em></td><td><a href="http://localhost:9000" target="_blank" rel="noopener noreferrer">http://localhost:9000</a></td></tr></tbody></table><p>Only two of these matter day to day: <strong>8000</strong> to play, <strong>5433</strong> to query.</p><p>The two Firebase emulators are visible in the Emulator UI at <a href="http://localhost:9000" target="_blank" rel="noopener noreferrer">http://localhost:9000</a>. Only <strong>Authentication</strong> and <strong>Storage</strong> run in this environment — the other cards showing <em>Off</em> is expected, not a sign that something failed:</p><p><img src="`+h+`" alt="The Firebase Emulator Suite overview, with the Authentication emulator on port 9099 and the Storage emulator on port 9199 both running"></p><div class="hint-container tip"><p class="hint-container-title">Port 5433, not 5432</p><p>The local database deliberately avoids the standard PostgreSQL port so it can run alongside a permanent development database on 5432. Every connection string below uses 5433.</p></div><h2 id="the-commands-you-need" tabindex="-1"><a class="header-anchor" href="#the-commands-you-need"><span>The commands you need</span></a></h2><p>Run all of these from the assessment&#39;s directory. The other scripts in <code>package.json</code> are for CI and platform developers — ignore them.</p><table><thead><tr><th>Command</th><th>What it does</th><th>When to use it</th><th>Data</th></tr></thead><tbody><tr><td><code>npm run setup</code></td><td>Check prerequisites, install, build libraries, create your config</td><td>Once, on a fresh clone</td><td>safe</td></tr><tr><td><code>npm start</code></td><td>Bring the stack up if needed, start this assessment&#39;s dev server</td><td>Every time you sit down to work</td><td>safe</td></tr><tr><td><code>npm run seed:tasks</code></td><td>Seed newly added variants into the <strong>running</strong> database</td><td>After editing your config</td><td>safe</td></tr><tr><td><code>npm run update</code></td><td>Rebuild the platform libraries on your machine</td><td>After a pull changes the SDK, schema, or scoring tables</td><td>safe</td></tr><tr><td><code>npm run rebuild</code></td><td>Force a clean rebuild of the Docker images</td><td>After a pull changes the backend or migrations</td><td>safe</td></tr><tr><td><code>npm stop</code></td><td>Stop everything <strong>and delete the database</strong></td><td>When you want a completely clean slate</td><td><strong>deletes</strong></td></tr><tr><td><code>npm restart</code></td><td>Full teardown <strong>and delete the database</strong>, then start fresh</td><td>When the stack is wedged</td><td><strong>deletes</strong></td></tr></tbody></table><h2 id="keeping-your-data-safe" tabindex="-1"><a class="header-anchor" href="#keeping-your-data-safe"><span>Keeping your data safe</span></a></h2><div class="hint-container caution"><p class="hint-container-title">Read this before you stop anything</p><p><code>npm stop</code> and <code>npm restart</code> <strong>delete your database</strong> — every run, trial, score, and uploaded recording. They do not mean what &quot;stop&quot; and &quot;restart&quot; usually mean.</p></div><table><thead><tr><th>Action</th><th>Stops</th><th>Your runs, trials, scores</th><th>Seeded variants</th></tr></thead><tbody><tr><td><strong>Ctrl+C</strong></td><td>Only the dev server on port 8000</td><td><strong>kept</strong> — the stack keeps running in the background</td><td>kept</td></tr><tr><td><strong><code>TRUNCATE app.runs CASCADE;</code></strong> <em>(in <code>roar_assessment</code>)</em></td><td>Nothing</td><td><strong>cleared, on purpose</strong> — trials, scores and interactions all cascade</td><td>kept, nothing to re-seed</td></tr><tr><td><strong><code>npm stop</code></strong></td><td>Every Docker service</td><td><strong>deleted</strong>, including uploaded recordings</td><td>deleted</td></tr><tr><td><strong><code>npm restart</code></strong></td><td>Everything, then starts fresh</td><td><strong>deleted</strong></td><td>deleted, then re-seeded</td></tr></tbody></table><p>Day to day you only need the first: Ctrl+C, walk away, come back, <code>npm start</code>. It reattaches to the same database and skips straight to the dev server.</p><p>Both destructive commands <strong>prompt for confirmation</strong>, and declining is completely clean — nothing is torn down, nothing starts, and no error is reported. Say no if you are unsure.</p><h2 id="producing-a-run" tabindex="-1"><a class="header-anchor" href="#producing-a-run"><span>Producing a run</span></a></h2><p>Open <a href="http://localhost:8000" target="_blank" rel="noopener noreferrer">http://localhost:8000</a>. On load, the page:</p><ol><li>Signs you in anonymously against the Firebase Auth emulator.</li><li>Provisions an anonymous ROAR participant.</li><li>Resolves a task variant.</li><li>Launches the assessment.</li></ol><p>Playing it through writes a run, its trials, and — for scored assessments — its scores. <strong>Play the assessment, and rows appear.</strong> There is nothing to submit or POST by hand.</p><h3 id="anonymous-participants-and-what-a-refresh-does" tabindex="-1"><a class="header-anchor" href="#anonymous-participants-and-what-a-refresh-does"><span>Anonymous participants, and what a refresh does</span></a></h3><p>Your anonymous identity is persisted by the browser, so:</p><ul><li><strong>A page refresh keeps the same participant.</strong> Repeated runs in the same browser accumulate under one identity, which is usually what you want while iterating.</li><li><strong>A fresh participant</strong> comes from a different browser or profile, an incognito window, cleared site data, or an emulator wipe (<code>npm restart</code> or <code>npm stop</code> — the Auth emulator is in-memory and reset by both).</li></ul><p>Anonymous runs have no participant name, but they do get an <code>assessment_pid</code>, which is the identifier worth querying on.</p><h2 id="choosing-what-runs-variants" tabindex="-1"><a class="header-anchor" href="#choosing-what-runs-variants"><span>Choosing what runs: variants</span></a></h2><p>A <strong>task</strong> is the assessment (Phonological Awareness). A <strong>variant</strong> is one configured version of it (English, adaptive, scoring version 5).</p><h3 id="the-variant-configuration-file" tabindex="-1"><a class="header-anchor" href="#the-variant-configuration-file"><span>The variant configuration file</span></a></h3><p>Each assessment reads a local <code>taskVariantParameters.json</code> to decide which variants to seed. The file is <strong>not committed</strong> — it is yours — and is required before the first start. <code>npm run setup</code> creates it from the committed example.</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json" data-title="json"><pre><code><span class="line"><span class="token punctuation">[</span></span>
<span class="line">  <span class="token punctuation">{</span></span>
<span class="line">    <span class="token property">&quot;variantName&quot;</span><span class="token operator">:</span> <span class="token string">&quot;English-v7&quot;</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token property">&quot;params&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">      <span class="token property">&quot;lng&quot;</span><span class="token operator">:</span> <span class="token string">&quot;en&quot;</span><span class="token punctuation">,</span></span>
<span class="line">      <span class="token property">&quot;scoringVersion&quot;</span><span class="token operator">:</span> <span class="token number">7</span><span class="token punctuation">,</span></span>
<span class="line">      <span class="token property">&quot;userMode&quot;</span><span class="token operator">:</span> <span class="token string">&quot;shortAdaptive&quot;</span><span class="token punctuation">,</span></span>
<span class="line">      <span class="token property">&quot;consent&quot;</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span></span>
<span class="line">      <span class="token property">&quot;skipInstructions&quot;</span><span class="token operator">:</span> <span class="token boolean">true</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The committed <code>taskVariantParameters.example.json</code> beside it documents <strong>every</strong> parameter with its valid values and sensible defaults. Start there.</p><p>For SWR, <code>lng</code> does something extra: it decides <strong>which task</strong> the variant belongs to. English and Spanish are separate tasks, not one task with a language setting — which is why SWR&#39;s example file holds five entries, one per language.</p><p>Validation happens when the variant is seeded, and a missing file, an unknown parameter key, or an invalid value fails with an error that <strong>names the offending key</strong>. Variants are matched by name, so seeding is idempotent and additive: an existing variant is skipped and a new entry is added alongside it.</p><div class="hint-container tip"><p class="hint-container-title">Changing a variant&#39;s parameters</p><p>Because variants are matched by name, editing the parameters under an existing <code>variantName</code> will not update the seeded variant. Give the changed variant a <strong>new name</strong>.</p></div><h3 id="seeding-a-new-variant-without-losing-data" tabindex="-1"><a class="header-anchor" href="#seeding-a-new-variant-without-losing-data"><span>Seeding a new variant without losing data</span></a></h3><div class="hint-container warning"><p class="hint-container-title">Editing the file does not seed it</p><p>The seed runs automatically only <strong>once</strong>, when the stack first comes up. Edit <code>taskVariantParameters.json</code> afterwards and run <code>npm start</code> again, and nothing happens — the stack is already up, so <code>npm start</code> skips straight to the dev server.</p><p><code>npm restart</code> does re-seed, but only because it deletes the database first, taking all of your generated data with it.</p></div><p>Use <code>npm run seed:tasks</code> instead. It runs the same additive seeder against the live database:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 1. Edit taskVariantParameters.json — add a new entry</span></span>
<span class="line"><span class="token comment"># 2. Seed it into the running environment (no teardown, no data loss)</span></span>
<span class="line"><span class="token function">npm</span> run seed:tasks</span>
<span class="line"><span class="token comment"># 3. Reload the assessment, or use the variant picker</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="the-variant-picker" tabindex="-1"><a class="header-anchor" href="#the-variant-picker"><span>The variant picker</span></a></h3><p>In development and staging — never in production — a small <strong>variant picker</strong> appears in the top-right corner of the assessment: a dropdown of the task&#39;s published variants. Selecting one reloads the page with that variant, preserving your other URL parameters.</p><p>It lists the same variants you seeded, so pair it with <code>npm run seed:tasks</code>: add a variant, seed it, reload, and it is in the dropdown. If a variant you expect is missing, it was never seeded.</p><p>The picker is scoped to the task(s) this assessment&#39;s dev server serves, so even with every assessment seeded into the shared database, one assessment&#39;s picker never shows another&#39;s variants.</p><h3 id="which-variant-runs-when-you-don-t-name-one" tabindex="-1"><a class="header-anchor" href="#which-variant-runs-when-you-don-t-name-one"><span>Which variant runs when you don&#39;t name one</span></a></h3><p>With no <code>variantId</code> in the URL, the assessment resolves its <strong>declared default variant</strong> — a per-task name in the assessment&#39;s <code>serve/serve.js</code>:</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token comment">// apps/assessments/roar-swr/serve/serve.js — one entry per language task</span></span>
<span class="line"><span class="token keyword">const</span> <span class="token constant">DEFAULT_VARIANT_NAMES</span> <span class="token operator">=</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token punctuation">[</span><span class="token constant">SWR_LANGUAGES</span><span class="token punctuation">.</span>en<span class="token punctuation">.</span>taskId<span class="token punctuation">]</span><span class="token operator">:</span> <span class="token string">&quot;English-v7&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token punctuation">[</span><span class="token constant">SWR_LANGUAGES</span><span class="token punctuation">.</span>es<span class="token punctuation">.</span>taskId<span class="token punctuation">]</span><span class="token operator">:</span> <span class="token string">&quot;Spanish-v1&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token comment">// …it, pt, de</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Resolution order is: <code>variantId</code> in the URL, then that declared default matched by name, then the oldest published variant if no default is declared.</p><div class="hint-container warning"><p class="hint-container-title">A mismatch is quiet</p><p>The committed values are placeholders taken from the example config. If you seed variants under names of your own, the declared default stops matching — and locally that is a <strong>console warning</strong>, not an error. You still get a run, just not the one you meant.</p><p>The warning lists every published variant name for the task, so <strong>check the browser console</strong> whenever a run does not look like the configuration you expected. To fix it, edit <code>DEFAULT_VARIANT_NAMES</code> to match your names.</p></div><p>Keep variant names simple — hyphenated ASCII such as <code>English-v7</code> rather than <code>English (v7)</code> — because names are matched by name and also passed on the command line.</p><h3 id="url-parameters" tabindex="-1"><a class="header-anchor" href="#url-parameters"><span>URL parameters</span></a></h3><p>Game parameters — adaptivity, item selection, language, scoring version — <strong>always come from the seeded variant</strong>. URL parameters only choose which variant runs and attach participant context.</p><table><thead><tr><th>Parameter</th><th>Purpose</th></tr></thead><tbody><tr><td><code>variantId</code></td><td>Run a specific seeded variant</td></tr><tr><td><code>participant</code></td><td>Participant ID (PID) to associate with the run</td></tr><tr><td><code>grade</code></td><td>Participant grade</td></tr><tr><td><code>birthyear</code>, <code>birthmonth</code>, <code>age</code>, <code>agemonths</code></td><td>Participant age or date-of-birth context</td></tr><tr><td><code>labId</code></td><td>Lab identifier</td></tr><tr><td><code>taskVersion</code></td><td>Task version string (defaults to <code>1.0</code>)</td></tr></tbody></table><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">http://localhost:8000/?variantId=&lt;id&gt;&amp;participant=demo-01&amp;grade=3</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Demographic parameters are not <code>run_demographics</code></p><p>That table is populated from rostering and stays <strong>empty for anonymous local runs</strong>. It also lives in <code>roar_core</code>, unlike every other <code>run_*</code> table. If an assessment records these values at all, they land in the run&#39;s <code>metadata</code>.</p></div><h2 id="querying-your-data" tabindex="-1"><a class="header-anchor" href="#querying-your-data"><span>Querying your data</span></a></h2><h3 id="two-databases" tabindex="-1"><a class="header-anchor" href="#two-databases"><span>Two databases</span></a></h3><p>Knowing which database holds what is the thing that most often trips people up.</p><table><thead><tr><th>Data</th><th>Database</th><th>Tables</th></tr></thead><tbody><tr><td>Participants, tasks, variants</td><td><code>roar_core</code></td><td><code>app.users</code>, <code>app.tasks</code>, <code>app.task_variants</code>, <code>app.task_variant_parameters</code></td></tr><tr><td>Runs, trials, scores, interactions</td><td><code>roar_assessment</code></td><td><code>app.runs</code>, <code>app.run_trials</code>, <code>app.run_scores</code>, <code>app.run_trial_interactions</code></td></tr><tr><td>Runs and scores, joinable against participants and tasks</td><td><code>roar_core</code></td><td><code>app_assessment_fdw.runs</code>, <code>app_assessment_fdw.run_scores</code></td></tr></tbody></table><p>In plain terms: <strong>core is &quot;who and what&quot;, assessment is &quot;what happened&quot;.</strong></p><p>The third row is what makes cross-database work possible — runs and scores are mirrored into <code>roar_core</code>, so a single query can join a score to the participant and task that produced it. <strong><code>run_trials</code> is not mirrored</strong>, so trial-level data is only queryable in <code>roar_assessment</code>.</p><p>Every table lives in the <code>app</code> schema, so prefix every table name with <code>app.</code> (or <code>app_assessment_fdw.</code> for the two mirrors).</p><h3 id="connecting-with-pgweb" tabindex="-1"><a class="header-anchor" href="#connecting-with-pgweb"><span>Connecting with PgWeb</span></a></h3><p>PgWeb is a SQL client that runs in a browser tab — no configuration files, no desktop application to set up.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># macOS</span></span>
<span class="line">brew <span class="token function">install</span> pgweb</span>
<span class="line"><span class="token comment"># Linux: download the release binary from https://github.com/sosedoff/pgweb/releases</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>There are two ways to point it at the database. Both end up in the same interface, so use whichever you find easier.</p><h4 id="option-1-fill-in-the-connection-form" tabindex="-1"><a class="header-anchor" href="#option-1-fill-in-the-connection-form"><span>Option 1: fill in the connection form</span></a></h4><p>Run <code>pgweb</code> with no arguments. It opens <a href="http://localhost:8081" target="_blank" rel="noopener noreferrer">http://localhost:8081</a> and shows a connection form — choose the <strong>Standard</strong> tab and enter:</p><table><thead><tr><th>Field</th><th>Value</th></tr></thead><tbody><tr><td>Host</td><td><code>localhost</code></td></tr><tr><td>Port</td><td><code>5433</code></td></tr><tr><td>Username</td><td><code>postgres</code></td></tr><tr><td>Password</td><td><em>leave blank</em></td></tr><tr><td>Database</td><td><code>roar_core</code> or <code>roar_assessment</code></td></tr><tr><td>SSL Mode</td><td><code>disable</code></td></tr></tbody></table><p><img src="`+u+`" alt="The PgWeb connection form, filled in for the local assessment database"></p><p>Then click <strong>Connect</strong>. To work in the other database, change <strong>Database</strong> and reconnect — or start a second PgWeb on another port so you can keep both open at once:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">pgweb <span class="token parameter variable">--listen</span> <span class="token number">8082</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h4 id="option-2-pass-the-connection-string" tabindex="-1"><a class="header-anchor" href="#option-2-pass-the-connection-string"><span>Option 2: pass the connection string</span></a></h4><p>If you would rather not retype the form, put the details in the command. This also makes it easy to open both databases at once, on two ports:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">pgweb <span class="token parameter variable">--url</span> <span class="token string">&quot;postgres://postgres@localhost:5433/roar_core?sslmode=disable&quot;</span></span>
<span class="line"><span class="token comment"># → http://localhost:8081</span></span>
<span class="line"></span>
<span class="line">pgweb <span class="token parameter variable">--url</span> <span class="token string">&quot;postgres://postgres@localhost:5433/roar_assessment?sslmode=disable&quot;</span> <span class="token parameter variable">--listen</span> <span class="token number">8082</span></span>
<span class="line"><span class="token comment"># → http://localhost:8082</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>Setting</th><th>Value</th></tr></thead><tbody><tr><td>Host</td><td><code>localhost</code></td></tr><tr><td>Port</td><td><code>5433</code></td></tr><tr><td>Username</td><td><code>postgres</code></td></tr><tr><td>Password</td><td><em>(none)</em></td></tr><tr><td>SSL mode</td><td><code>disable</code></td></tr></tbody></table><p>The password is blank on purpose: this database is local, disposable, and holds no real participant data. If you prefer other tools, <code>psql</code>, <code>pgcli</code>, and PgAdmin all connect with the same details.</p><h3 id="the-query-catalog" tabindex="-1"><a class="header-anchor" href="#the-query-catalog"><span>The query catalog</span></a></h3><table><thead><tr><th>What you want to know</th><th>Run it in</th></tr></thead><tbody><tr><td>Which variants did I seed, and what are their IDs?</td><td><code>roar_core</code></td></tr><tr><td>All runs for a task, with the participant</td><td><code>roar_core</code></td></tr><tr><td>Trial-by-trial detail for one run</td><td><code>roar_assessment</code></td></tr><tr><td>Scores for completed runs</td><td><code>roar_assessment</code></td></tr><tr><td>Run summary — trial counts, engagement, reliability</td><td><code>roar_assessment</code></td></tr><tr><td>Engagement events — focus/blur, fullscreen</td><td><code>roar_assessment</code></td></tr><tr><td>Scores joined to participant, task, and run metadata</td><td><code>roar_core</code></td></tr></tbody></table>`,94)),s("p",null,[e[4]||(e[4]=t("All seven are written out ready to paste in the ")),a(n,{to:"/researcher/assessment-environment/quick-reference.html#queries"},{default:o(()=>e[3]||(e[3]=[t("Quick Reference")])),_:1}),e[5]||(e[5]=t(". The two you will use most are below."))]),e[17]||(e[17]=r(`<h3 id="which-runs-exist-and-who-produced-them" tabindex="-1"><a class="header-anchor" href="#which-runs-exist-and-who-produced-them"><span>Which runs exist, and who produced them</span></a></h3><p>Run in <code>roar_core</code>:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span></span>
<span class="line">  r<span class="token punctuation">.</span>id<span class="token punctuation">,</span></span>
<span class="line">  u<span class="token punctuation">.</span>assessment_pid<span class="token punctuation">,</span></span>
<span class="line">  r<span class="token punctuation">.</span>is_anonymous<span class="token punctuation">,</span></span>
<span class="line">  r<span class="token punctuation">.</span>completed_at<span class="token punctuation">,</span></span>
<span class="line">  r<span class="token punctuation">.</span>reliable_run<span class="token punctuation">,</span></span>
<span class="line">  r<span class="token punctuation">.</span>metadata</span>
<span class="line"><span class="token keyword">FROM</span> app_assessment_fdw<span class="token punctuation">.</span>runs r</span>
<span class="line"><span class="token keyword">JOIN</span> app<span class="token punctuation">.</span>users u <span class="token keyword">ON</span> u<span class="token punctuation">.</span>id <span class="token operator">=</span> r<span class="token punctuation">.</span>user_id</span>
<span class="line"><span class="token keyword">JOIN</span> app<span class="token punctuation">.</span>task_variants tv <span class="token keyword">ON</span> tv<span class="token punctuation">.</span>id <span class="token operator">=</span> r<span class="token punctuation">.</span>task_variant_id</span>
<span class="line"><span class="token keyword">JOIN</span> app<span class="token punctuation">.</span>tasks t <span class="token keyword">ON</span> t<span class="token punctuation">.</span>id <span class="token operator">=</span> tv<span class="token punctuation">.</span>task_id</span>
<span class="line"><span class="token keyword">WHERE</span> t<span class="token punctuation">.</span>slug <span class="token operator">=</span> <span class="token string">&#39;swr&#39;</span>          <span class="token comment">-- English only; LIKE &#39;swr%&#39; for all five</span></span>
<span class="line">  <span class="token operator">AND</span> r<span class="token punctuation">.</span>deleted_at <span class="token operator">IS</span> <span class="token boolean">NULL</span></span>
<span class="line"><span class="token keyword">ORDER</span> <span class="token keyword">BY</span> r<span class="token punctuation">.</span>created_at <span class="token keyword">DESC</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="what-happened-inside-one-run" tabindex="-1"><a class="header-anchor" href="#what-happened-inside-one-run"><span>What happened inside one run</span></a></h3><p>Run in <code>roar_assessment</code>, using a <code>run_id</code> from the previous query:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span></span>
<span class="line">  trial_num_total<span class="token punctuation">,</span></span>
<span class="line">  item<span class="token punctuation">,</span></span>
<span class="line">  correct<span class="token punctuation">,</span></span>
<span class="line">  response<span class="token punctuation">,</span></span>
<span class="line">  response_time_ms<span class="token punctuation">,</span></span>
<span class="line">  subtask<span class="token punctuation">,</span></span>
<span class="line">  assessment_stage</span>
<span class="line"><span class="token keyword">FROM</span> app<span class="token punctuation">.</span>run_trials</span>
<span class="line"><span class="token keyword">WHERE</span> run_id <span class="token operator">=</span> <span class="token string">&#39;&lt;your-run-id&gt;&#39;</span></span>
<span class="line"><span class="token keyword">ORDER</span> <span class="token keyword">BY</span> trial_index<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Always filter soft-deleted rows</p><p>Records are soft-deleted rather than removed, so include <code>AND deleted_at IS NULL</code> or you may pull rows that are meant to be gone.</p></div><h3 id="two-things-that-silently-mislead" tabindex="-1"><a class="header-anchor" href="#two-things-that-silently-mislead"><span>Two things that silently mislead</span></a></h3><p><strong>Task slugs are not always a single value.</strong> Some assessments treat language as a separate task — SWR is five tasks, not one (<code>swr</code>, <code>swr-es</code>, <code>swr-it</code>, <code>swr-pt</code>, <code>swr-de</code>) — and multi-task assessments have several slugs. Matching with <code>=</code> gives a clean-looking result set that is quietly incomplete:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">WHERE</span> t<span class="token punctuation">.</span>slug <span class="token operator">=</span> <span class="token string">&#39;swr&#39;</span>      <span class="token comment">-- misses every Spanish run, silently</span></span>
<span class="line"><span class="token keyword">WHERE</span> t<span class="token punctuation">.</span>slug <span class="token operator">LIKE</span> <span class="token string">&#39;swr%&#39;</span>  <span class="token comment">-- matches the family</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Assessment-specific fields live in <code>metadata</code>.</strong> Known trial fields become real columns; everything else a trial emits lands in <code>run_trials.metadata</code>. Run-level context lands in <code>runs.metadata</code>. <code>run_scores</code> has <strong>no</strong> metadata column — join a score to its run and read the run&#39;s metadata instead.</p><p>Because trial metadata is assessment-specific, discover the keys before extracting them:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> <span class="token keyword">DISTINCT</span> jsonb_object_keys<span class="token punctuation">(</span>metadata<span class="token punctuation">)</span> <span class="token keyword">AS</span> metadata_key</span>
<span class="line"><span class="token keyword">FROM</span> app<span class="token punctuation">.</span>run_trials</span>
<span class="line"><span class="token keyword">WHERE</span> run_id <span class="token operator">=</span> <span class="token string">&#39;&lt;your-run-id&gt;&#39;</span></span>
<span class="line">  <span class="token operator">AND</span> metadata <span class="token operator">IS</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Then pull fields alongside real columns with <code>metadata -&gt;&gt; &#39;fieldName&#39;</code> for text, or <code>metadata -&gt; &#39;nested&#39;</code> to keep JSON. To filter, <code>-&gt;&gt;</code> compares as text and <code>@&gt;</code> does a JSON containment match.</p><h2 id="exporting-to-csv" tabindex="-1"><a class="header-anchor" href="#exporting-to-csv"><span>Exporting to CSV</span></a></h2><p>Any query above can be exported.</p><ul><li><p><strong>PgWeb</strong> — run the query, then use the export control on the results toolbar and choose CSV. Best for one-off exports while exploring.</p></li><li><p><strong><code>psql \\copy</code></strong> — scriptable, and it writes to the machine where <code>psql</code> runs, so the file lands in your current directory. The whole command must be on one physical line:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line">\\copy <span class="token punctuation">(</span><span class="token keyword">SELECT</span> trial_num_total<span class="token punctuation">,</span> item<span class="token punctuation">,</span> correct <span class="token keyword">FROM</span> app<span class="token punctuation">.</span>run_trials <span class="token keyword">WHERE</span> run_id <span class="token operator">=</span> <span class="token string">&#39;&lt;your-run-id&gt;&#39;</span> <span class="token keyword">ORDER</span> <span class="token keyword">BY</span> trial_index<span class="token punctuation">)</span> <span class="token keyword">TO</span> <span class="token string">&#39;trials.csv&#39;</span> <span class="token keyword">WITH</span> <span class="token punctuation">(</span>FORMAT csv<span class="token punctuation">,</span> HEADER<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><p><strong>One-liner from the shell</strong> — the same result, handy for repeatable exports:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">psql <span class="token string">&quot;postgres://postgres@localhost:5433/roar_assessment?sslmode=disable&quot;</span> <span class="token parameter variable">--csv</span> <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token parameter variable">-c</span> <span class="token string">&quot;SELECT trial_num_total, item, correct FROM app.run_trials WHERE run_id = &#39;&lt;your-run-id&gt;&#39; ORDER BY trial_index&quot;</span> <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token operator">&gt;</span> trials.csv</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul><p>Pick the database that matches the query: trial-level exports run against <code>roar_assessment</code>, while participant and score joins run against <code>roar_core</code>. For a <strong>single CSV combining participants, scores, and run metadata</strong>, export the cross-database join from <code>roar_core</code>.</p><h2 id="resetting-your-generated-data" tabindex="-1"><a class="header-anchor" href="#resetting-your-generated-data"><span>Resetting your generated data</span></a></h2><p>To clear the runs, trials, scores, and interactions you have generated while <strong>keeping your seeded variants</strong>, truncate the run tables in <code>roar_assessment</code>:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">TRUNCATE</span> app<span class="token punctuation">.</span>runs <span class="token keyword">CASCADE</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Trials, scores, and interactions all cascade from <code>runs</code>, so this one statement clears everything you generated. Your tasks and variants in <code>roar_core</code> are untouched, so there is nothing to re-seed — far faster than a full teardown.</p><h2 id="switching-between-assessments" tabindex="-1"><a class="header-anchor" href="#switching-between-assessments"><span>Switching between assessments</span></a></h2><p>The stack is shared and persistent, so moving from one assessment to another tears nothing down:</p><ol><li><strong>Ctrl+C</strong> — frees port 8000; the stack and your data stay up.</li><li><strong><code>cd ../roar-pa</code></strong> (or whichever assessment you want).</li><li><strong><code>npm run seed:tasks</code></strong> — first visit only. Create its config first if you never have, by copying its example file.</li><li><strong><code>npm start</code></strong> — same stack, same database.</li></ol><p>Switching back needs no re-seed. Once an assessment is seeded, returning to it is just Ctrl+C, <code>cd</code>, <code>npm start</code>. You do not need to run <code>npm run setup</code> again — and doing so mid-switch would spuriously report port 5433 as in use, which is only your own running stack.</p><h2 id="recordings-audio-and-video-assessments" tabindex="-1"><a class="header-anchor" href="#recordings-audio-and-video-assessments"><span>Recordings (audio and video assessments)</span></a></h2><p>Assessments that capture audio or video — Read Aloud, for instance — upload through the SDK to the local <strong>Firebase Storage emulator</strong>, so no cloud credentials are needed and no real bucket is touched.</p><p>Browse them in the <strong>Emulator UI</strong> at <a href="http://localhost:9000" target="_blank" rel="noopener noreferrer">http://localhost:9000</a> under the <strong>Storage</strong> tab. The emulated bucket is <code>demo-roar.appspot.com</code>, created on the first upload — each assessment that has uploaded anything appears as a top-level folder named after its task.</p><p><img src="`+m+`" alt="The Storage tab of the Firebase Emulator UI, showing the demo-roar.appspot.com bucket with a roar-readaloud folder"></p><p>Recordings are written to a deterministic path:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">{taskId}/{participantId}/{assessmentPid}/{administrationId}/{runId}/{filename}</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The third segment falls back to <code>participantId</code> when a run has no PID, so for an anonymous local run those two segments often match.</p><p>Clicking down through those folders gets you to the recordings themselves. The breadcrumb shows the whole path, and each file carries the size and content type the SDK uploaded it with:</p><p><img src="`+g+'" alt="The nested run folder in the Storage tab, listing three audio/webm recordings with their sizes"></p><p>Each recording&#39;s <code>gs://</code> reference is also written onto its trial. It is not a standard trial column, so it lands in <code>run_trials.metadata</code> — for Read Aloud, under the key <code>uploadUrl</code>.</p><div class="hint-container caution"><p class="hint-container-title">Recordings are held in memory</p><p>The Storage emulator has no import or export configured. Recordings survive <strong>Ctrl+C</strong>, which stops only the dev server, but are cleared when the emulator container restarts (<code>npm restart</code>) or the environment is torn down (<code>npm stop</code>). Download anything you need to keep.</p></div><h2 id="keeping-current-after-a-pull" tabindex="-1"><a class="header-anchor" href="#keeping-current-after-a-pull"><span>Keeping current after a pull</span></a></h2><table><thead><tr><th>What changed in the pull</th><th>Run</th><th>Why</th></tr></thead><tbody><tr><td>The SDK, assessment schema, or scoring tables</td><td><code>npm run update</code></td><td>Your dev server bundles these from your machine — then restart the dev server</td></tr><tr><td>The backend, migrations, API contract, Dockerfile, or root dependencies</td><td><code>npm run rebuild</code></td><td>These live inside the Docker images, and Docker caches aggressively</td></tr><tr><td>The assessment schema</td><td>both</td><td>It is used by the dev server <em>and</em> the backend</td></tr><tr><td>You are not sure — it was a big pull</td><td><code>npm run rebuild</code>, then <code>npm run update</code></td><td>The safe combination</td></tr></tbody></table><p>Neither command deletes anything, and the environment does not need to be stopped first for a rebuild. The symptom that means you need one of these is a change you know landed that is not taking effect.</p><h2 id="troubleshooting" tabindex="-1"><a class="header-anchor" href="#troubleshooting"><span>Troubleshooting</span></a></h2><table><thead><tr><th>Symptom</th><th>What it means, and the fix</th></tr></thead><tbody><tr><td><strong>&quot;Port 5433 is already in use&quot;</strong></td><td>Usually a leftover container — <code>docker ps | grep 5433</code>. Stop it, or start with <code>ASSESSMENT_PG_PORT=&lt;port&gt; npm start</code>.</td></tr><tr><td><strong>&quot;Port 8000 is already in use&quot;</strong></td><td>A previous dev server, possibly for another assessment, is still running. Stop that process.</td></tr><tr><td><strong>&quot;taskVariantParameters.json not found&quot;</strong></td><td>The config step was skipped. Run <code>npm run setup</code>, or copy the example file yourself.</td></tr><tr><td><strong>Migration container failed / &quot;Unknown task&quot;</strong></td><td>The assessment is not registered, or a parameter is invalid. The error names the available tasks and the offending key. Fix it, then <code>npm run rebuild</code> and <code>npm start</code>.</td></tr><tr><td><strong>&quot;My new variant didn&#39;t show up&quot;</strong></td><td>Editing the config does not re-seed. Run <code>npm run seed:tasks</code> — <em>not</em> <code>npm restart</code>, which would wipe your data.</td></tr><tr><td><strong>The assessment isn&#39;t loading the variant I expected</strong></td><td>The declared default probably does not match your variant names. Check the browser console; the warning lists every published variant.</td></tr><tr><td><strong>A code change isn&#39;t taking effect</strong></td><td>Host library change → <code>npm run update</code>. Backend or migration change → <code>npm run rebuild</code>.</td></tr><tr><td><strong>The Firebase emulator won&#39;t start / &quot;failed to bind host port 9000&quot;</strong></td><td>Another Firebase emulator already owns 9000, 9099 or 9199 — most often a persistent platform-dev stack. Stop it, then <code>npm start</code>. If the container was created during the failed attempt it can restart without republishing its ports, so recreate it: <code>docker compose -f docker-compose.assessment.yml up -d --force-recreate firebase-emulator</code></td></tr><tr><td><strong>Stale containers, name or port conflicts on start</strong></td><td><code>npm start</code> clears known stale containers first. Still wedged? <code>npm stop</code> then <code>npm start</code> — but that deletes your data.</td></tr><tr><td><strong><code>docker stop</code> fails with &quot;permission denied&quot; (Linux)</strong></td><td><code>npm stop</code> falls back to direct process kills and prints the exact <code>sudo kill</code> command to run. Run it, then re-run <code>npm stop</code>.</td></tr></tbody></table><p>Two messages during seeding look like problems but are not:</p><ul><li><strong><code>Launch sandbox administration not found — skipping assignment</code></strong> — four assessments declare a default variant for the dashboard&#39;s development sandbox, which is not seeded in this environment. Your variants are still seeded and playable.</li><li><strong><code>Config &quot;…&quot; names defaultVariant &quot;…&quot;, which is not in the parameters file</code></strong> — you renamed your variants, so only that sandbox assignment was skipped. It does not affect what <code>localhost:8000</code> loads.</li></ul><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2>',45)),s("ul",null,[s("li",null,[e[7]||(e[7]=t("The ")),a(n,{to:"/researcher/assessment-environment/quick-reference.html"},{default:o(()=>e[6]||(e[6]=[t("Quick Reference")])),_:1}),e[8]||(e[8]=t(", for the commands, connection details, and every query on one page."))]),e[9]||(e[9]=s("li",null,[t("The "),s("a",{href:"https://github.com/yeatmanlab/roar-dashboard/blob/main/apps/assessments/ASSESSMENT_ENVIRONMENT.md",target:"_blank",rel:"noopener noreferrer"},"Setup & Operations guide"),t(" and "),s("a",{href:"https://github.com/yeatmanlab/roar-dashboard/blob/main/apps/assessments/ASSESSMENT_RESEARCH_GUIDE.md",target:"_blank",rel:"noopener noreferrer"},"Research guide"),t(" in the repository, for the complete reference.")],-1)),e[10]||(e[10]=s("li",null,[s("a",{href:"https://yeatmanlab.github.io/roar-docs/researcher/data-tools/",target:"_blank",rel:"noopener noreferrer"},"Data Tools"),t(" for working with ROAR's cloud data in BigQuery.")],-1)),e[11]||(e[11]=s("li",null,[s("a",{href:"https://yeatmanlab.github.io/roar-docs/researcher/data-cleaning/",target:"_blank",rel:"noopener noreferrer"},"Data Cleaning"),t(" for the shared conventions on preparing ROAR data for analysis.")],-1))]),e[18]||(e[18]=s("p",null,[t("If something on this page was unclear enough that you had to ask someone, that is worth reporting — open a pull request on "),s("a",{href:"https://github.com/yeatmanlab/roar-docs",target:"_blank",rel:"noopener noreferrer"},"roar-docs"),t(", or raise it with the platform team.")],-1))])}const f=l(v,[["render",b],["__file","index.html.vue"]]),w=JSON.parse(`{"path":"/researcher/assessment-environment/","title":"Assessment Environment","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"What this gives you","slug":"what-this-gives-you","link":"#what-this-gives-you","children":[]},{"level":2,"title":"The research loop","slug":"the-research-loop","link":"#the-research-loop","children":[]},{"level":2,"title":"Before you start","slug":"before-you-start","link":"#before-you-start","children":[]},{"level":2,"title":"First-time setup","slug":"first-time-setup","link":"#first-time-setup","children":[]},{"level":2,"title":"Everyday use","slug":"everyday-use","link":"#everyday-use","children":[{"level":3,"title":"One shared stack, many assessments","slug":"one-shared-stack-many-assessments","link":"#one-shared-stack-many-assessments","children":[]},{"level":3,"title":"What's running, and where","slug":"what-s-running-and-where","link":"#what-s-running-and-where","children":[]}]},{"level":2,"title":"The commands you need","slug":"the-commands-you-need","link":"#the-commands-you-need","children":[]},{"level":2,"title":"Keeping your data safe","slug":"keeping-your-data-safe","link":"#keeping-your-data-safe","children":[]},{"level":2,"title":"Producing a run","slug":"producing-a-run","link":"#producing-a-run","children":[{"level":3,"title":"Anonymous participants, and what a refresh does","slug":"anonymous-participants-and-what-a-refresh-does","link":"#anonymous-participants-and-what-a-refresh-does","children":[]}]},{"level":2,"title":"Choosing what runs: variants","slug":"choosing-what-runs-variants","link":"#choosing-what-runs-variants","children":[{"level":3,"title":"The variant configuration file","slug":"the-variant-configuration-file","link":"#the-variant-configuration-file","children":[]},{"level":3,"title":"Seeding a new variant without losing data","slug":"seeding-a-new-variant-without-losing-data","link":"#seeding-a-new-variant-without-losing-data","children":[]},{"level":3,"title":"The variant picker","slug":"the-variant-picker","link":"#the-variant-picker","children":[]},{"level":3,"title":"Which variant runs when you don't name one","slug":"which-variant-runs-when-you-don-t-name-one","link":"#which-variant-runs-when-you-don-t-name-one","children":[]},{"level":3,"title":"URL parameters","slug":"url-parameters","link":"#url-parameters","children":[]}]},{"level":2,"title":"Querying your data","slug":"querying-your-data","link":"#querying-your-data","children":[{"level":3,"title":"Two databases","slug":"two-databases","link":"#two-databases","children":[]},{"level":3,"title":"Connecting with PgWeb","slug":"connecting-with-pgweb","link":"#connecting-with-pgweb","children":[]},{"level":3,"title":"The query catalog","slug":"the-query-catalog","link":"#the-query-catalog","children":[]},{"level":3,"title":"Which runs exist, and who produced them","slug":"which-runs-exist-and-who-produced-them","link":"#which-runs-exist-and-who-produced-them","children":[]},{"level":3,"title":"What happened inside one run","slug":"what-happened-inside-one-run","link":"#what-happened-inside-one-run","children":[]},{"level":3,"title":"Two things that silently mislead","slug":"two-things-that-silently-mislead","link":"#two-things-that-silently-mislead","children":[]}]},{"level":2,"title":"Exporting to CSV","slug":"exporting-to-csv","link":"#exporting-to-csv","children":[]},{"level":2,"title":"Resetting your generated data","slug":"resetting-your-generated-data","link":"#resetting-your-generated-data","children":[]},{"level":2,"title":"Switching between assessments","slug":"switching-between-assessments","link":"#switching-between-assessments","children":[]},{"level":2,"title":"Recordings (audio and video assessments)","slug":"recordings-audio-and-video-assessments","link":"#recordings-audio-and-video-assessments","children":[]},{"level":2,"title":"Keeping current after a pull","slug":"keeping-current-after-a-pull","link":"#keeping-current-after-a-pull","children":[]},{"level":2,"title":"Troubleshooting","slug":"troubleshooting","link":"#troubleshooting","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1788279264000,"contributors":[{"name":"Kyle Montville","username":"Kyle Montville","email":"kmontvil@stanford.edu","commits":1,"url":"https://github.com/Kyle Montville"}]},"filePathRelative":"researcher/assessment-environment/README.md"}`);export{f as comp,w as data};
